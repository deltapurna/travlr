<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Travlr" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="name">
          <h1><a href="#">Travlr</a></h1>
        </li>
      </ul>

      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
          <li class="active"><a href="#">Hello, <%= current_user ? current_user.name : 'Guest' %></a></li>
        </ul>

        <!-- Left Nav Section -->
        <ul class="left">
          <% if current_user %>
          <li><%= link_to 'Log out', logout_path, method: 'delete' %></li>
        <% else %>
          <li><%= link_to 'Sign in with Twitter', '/auth/twitter' %></li>
          <li><%= link_to 'Sign in', '/auth/identity' %></li>
        <% end %>
        </ul>
      </section>
    </nav>
    <% if notice %>
      <div data-alert class="alert-box success ra">
        <%= notice %>
        <a href="#" class="close">&times;</a>
      </div>
    <% end %>
    <% if alert %>
      <div data-alert class="alert-box alert ra">
        <%= alert %>
        <a href="#" class="close">&times;</a>
      </div>
    <% end %>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
